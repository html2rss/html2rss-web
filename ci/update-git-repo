#!/usr/bin/env bash
set -e

echo update-git-repo

# https://docs.travis-ci.com/user/best-practices-security/#recommendations-on-how-to-avoid-leaking-secrets-to-build-logs

git add Gemfile.lock
echo 'would comit and push now'
