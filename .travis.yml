sudo: required

services:
  - docker

addons:
  apt:
    packages:
      - docker-ce

before_install:
  - bash docker/build

deploy:
  provider: script
  script: bash docker/push
  on:
    branch: master
