{
  "name": "html2rss-frontend",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "astro dev --port 3001 --host",
    "build": "astro build",
    "preview": "astro preview",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "vitest",
    "test:run": "vitest run",
    "test:unit": "vitest run --reporter=verbose --exclude='**/*integration*.test.js'",
    "test:integration": "node scripts/test-with-server.cjs",
    "test:ci": "npm run test:unit && npm run test:integration"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.35.3",
    "astro": "^5.13.8",
    "tslib": "^2.8.1"
  },
  "devDependencies": {
    "prettier": "^3.x.x",
    "prettier-plugin-astro": "^0.x.x",
    "vitest": "^1.0.0"
  }
}
